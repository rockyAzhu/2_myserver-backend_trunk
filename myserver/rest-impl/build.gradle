apply plugin: 'pegasus' // defines compile project()

ext.apiProject = project(':myserver:rest-api')
dependencies {
    // ComplexKeyResourceTemplate, CollectionResourceTemplate,
    // @RestLiCollection
    compile 'com.linkedin.pegasus:restli-server:6.0.12'

    compile project(':myserver:rest-api')
}
